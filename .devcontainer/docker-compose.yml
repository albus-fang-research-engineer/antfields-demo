services:
  antfields-baseline:
    # image: antfields:demo
    container_name: antfields_container_baseline
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    stdin_open: true
    tty: true
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /home/albusfang/Albus/antfields-demo-baseline:/antfields-baseline
    # gpus: all
    runtime: nvidia
    command: /bin/bash
